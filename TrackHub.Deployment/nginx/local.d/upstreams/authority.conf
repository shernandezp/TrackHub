# TrackHub local upstream: authority_server
# DEFAULT: routed to the Docker container "authority".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream authority_server {
    server authority:8080;
}
