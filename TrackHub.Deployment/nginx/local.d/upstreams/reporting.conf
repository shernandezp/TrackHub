# TrackHub local upstream: reporting_api
# DEFAULT: routed to the Docker container "reporting".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream reporting_api {
    server reporting:8080;
}
