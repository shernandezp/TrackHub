# TrackHub local upstream: geofencing_api
# DEFAULT: routed to the Docker container "geofencing".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream geofencing_api {
    server geofencing:8080;
}
