# TrackHub local upstream: trip_api
# DEFAULT: routed to the Docker container "tripmanagement".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream trip_api {
    server tripmanagement:8080;
}
