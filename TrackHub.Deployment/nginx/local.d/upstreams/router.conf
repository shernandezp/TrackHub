# TrackHub local upstream: router_api
# DEFAULT: routed to the Docker container "router".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream router_api {
    server router:8080;
}
