# TrackHub local upstream: telemetry_api
# DEFAULT: routed to the Docker container "telemetry".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream telemetry_api {
    server telemetry:8080;
}
