# TrackHub local upstream: manager_api
# DEFAULT: routed to the Docker container "manager".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream manager_api {
    server manager:8080;
}
