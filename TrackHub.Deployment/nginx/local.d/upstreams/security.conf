# TrackHub local upstream: security_api
# DEFAULT: routed to the Docker container "security".
# Set-DebugTarget.ps1 rewrites this file to point at a service running in Visual Studio.
upstream security_api {
    server security:8080;
}
